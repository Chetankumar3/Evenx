# EvenX frontend environment variables
# Copy this file to `.env` and adjust as needed.

# Base URL of the main (Node/Express) HTTP API.
VITE_API_BASE_URL=http://localhost:3000

# Base URL of the statesync WebSocket service.
VITE_WS_BASE_URL=ws://localhost:8080
